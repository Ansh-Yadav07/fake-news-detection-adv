export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function ResultCard({ title, data export default function an>export default funcsName="text-xs text-zinc-500 font-medium">Confidence Score</span>
      </div>

      <div className="h-[6px] w-full bg-zinc-100 rounded-full overflow-hidden mt-4">
        <div 
          className="h-full bg-zinc-900 rounded-full transition-all duration-[1200ms] ease-out"
          style={{ width: `${data.confidence}%` }}
        />
      </div>
    </div>
  );
}
